import { ResizePayload } from "./types";

export const CUSTOM_NODE_KEY = "isPhosphorIcon";

export const WINDOW_SIZE_KEY = "window_size";
export const DEFAULT_WINDOW_SIZE: ResizePayload = { width: 362, height: 490 };
export const MINIMUM_WINDOW_SIZE: ResizePayload = { width: 240, height: 276 };

export const DEFAULT_SIZE = 32;
